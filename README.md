# 🤖 ربات تلگرام آرشیو محتوا و احراز هویت (Archive Bot)

این یک ربات تلگرامی پیشرفته نوشته شده با **Python** است که برای مدیریت دسترسی به محتوا و جمع‌آوری شماره تماس کاربران (Lead Generation) طراحی شده است. کاربران برای دسترسی به فایل‌ها باید ابتدا شماره تماس خود را تایید کنند. همچنین این ربات دارای یک **پنل مدیریت (Admin Panel)** کامل است که امکان آپلود و حذف فایل‌ها را بدون نیاز به کدنویسی فراهم می‌کند.

## ✨ ویژگی‌ها

- **🔐 احراز هویت اجباری:** کاربران تا زمان ارسال شماره تماس (از طریق دکمه مخصوص تلگرام) به محتوا دسترسی ندارند.
- **🗄 دیتابیس SQLite:** ذخیره اطلاعات کاربران و محتواها به صورت دائمی.
- **🔧 پنل مدیریت قدرتمند:**
  - مشاهده لیست کاربران ثبت‌نام شده (نام، آیدی، شماره تماس).
  - افزودن محتوا (متن، عکس، ویدیو، فایل، ویس) به صورت داینامیک.
  - حذف دسته‌بندی‌ها و فایل‌ها.
- **📂 ساختار ماژولار:** تفکیک کد اصلی (`main.py`) از کدهای دیتابیس (`database.py`).
- **⚡️ سرعت بالا:** استفاده از سیستم Cache برای کاهش درخواست‌ها به دیتابیس.

---

## 🛠 پیش‌نیازها

برای اجرای این ربات به موارد زیر نیاز دارید:

- **Python 3.x**
- کتابخانه **pyTelegramBotAPI**

## 📥 نصب و راه‌اندازی

۱. مخزن را کلون کنید یا فایل‌ها را دانلود کنید:
```bash
git clone https://github.com/arvinDev86/verify-number-and-uploader-telegramBot.git

cd verify-number-and-uploader-telegramBot
